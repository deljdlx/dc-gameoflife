services:
  gameofile:
    image: nginx:alpine
    restart: always
    env_file:
      - .env
    volumes:
      - "./src:/usr/share/nginx/html"
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=web"
      - "traefik.http.routers.gameofile.rule=Host(\"${DOMAIN}\")"
      - "traefik.http.routers.gameofile.entrypoints=websecure"
      - "traefik.http.routers.gameofile.tls.certresolver=myresolver"
    networks:
      - web

networks:
  web:
    name: web
    external: true
